---
"@neovici/cosmoz-bottom-bar": minor
---

Slimmer, flat toolbar buttons

Toolbar actions and the overflow (⋮) button are 32px tall (was 40px), with 13px
medium-weight text and a plain `--cz-shadow-xs` instead of the skeuomorphic
shadow. This matches the default `cosmoz-button`.

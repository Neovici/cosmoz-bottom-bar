---
"@neovici/cosmoz-bottom-bar": minor
---

Make toolbar buttons and the overflow menu button a neutral dark gray instead of brand blue

Matches the neutral primary `cosmoz-button`: `gray-800` (`gray-700` in dark
mode), one step lighter on hover.

---
"@neovici/cosmoz-bottom-bar": major
---

Migrate bottom-bar styling to cosmoz-tokens design tokens (`--cz-*`) and drop legacy `--cosmoz-bottom-bar-button-*` CSS custom properties; modernize dependencies. Slotted buttons are now expected to use token-based styles (@neovici/cosmoz-button, @neovici/cosmoz-dropdown ^8).
